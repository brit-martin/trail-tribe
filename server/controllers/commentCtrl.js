import { Comment, User, Post } from '../models/commentModel.js'


export default {
    comment: async (req, res) => {
        console.log('comment router')
        // const comment = await Comment. 
    }
}